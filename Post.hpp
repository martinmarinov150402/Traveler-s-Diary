#include "User.hpp"
class Post
{
    private:
        int id;
        char authorName[MAX_USERNAME_SIZE];
        int grade;
        char dateStart[11];
        char dateEnd[11];
        char comment[1001];

        
};